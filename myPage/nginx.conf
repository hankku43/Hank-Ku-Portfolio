limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/m;

server {
    listen 80;
    server_name hankku.site;

    root /usr/share/nginx/html;
    index index.html;

    # Let's Encrypt ACME challenge (certbot renew 用)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # 反向代理 /api/* → FastAPI backend 容器
    location /api {
        limit_req zone=api_limit burst=5 nodelay;
        limit_req_status 429;
        proxy_pass         http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # Vue SPA fallback — 讓 vue-router history mode 正常運作
    location / {
        try_files $uri $uri/ /index.html;
    }
}

# ── HTTPS (啟用前需先用 certbot 取得憑證) ───────────────────────────────────
# server {
#     listen 443 ssl;
#     server_name hankku.site;
#
#     ssl_certificate     /etc/letsencrypt/live/hankku.site/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/hankku.site/privkey.pem;
#
#     root /usr/share/nginx/html;
#     index index.html;
#
#     location /api {
#         limit_req zone=api_limit burst=5 nodelay;
#         limit_req_status 429;
#         proxy_pass         http://backend:8000;
#         proxy_http_version 1.1;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#     }
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# }
