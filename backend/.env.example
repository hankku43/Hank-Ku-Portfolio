# ── Model paths (relative to backend/ root) ──────────────────────────────────
DIGIT_MODEL_PATH=models/digit_recognition.pth
PTT_MODEL_PATH=models/ptt_classifier.pth
PTT_DOC2VEC_PATH=models/ptt_titles_doc2vec_ver3.model
RAG_DOC2VEC_PATH=models/traffic_law_doc2vec.model
CLASSICAL_NLP_MODEL_PATH=models/classical_chinese_lm.pth

# ── External APIs ─────────────────────────────────────────────────────────────
# Ollama endpoint — RAG 功能需要。本機預設 http://localhost:11434
OLLAMA_BASE_URL=http://localhost:11434

# ── Server ────────────────────────────────────────────────────────────────────
# Comma-separated allowed origins, or * for all
CORS_ORIGINS=["http://localhost:5173","https://your-portfolio-domain.com"]
